// Route constants
export const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  PROFILE: '/profile',
  SETTINGS: '/settings',
  POST: '/post/:id',
  TAG: '/tag/:slug',
  CATEGORY: '/category/:slug',
  NOT_FOUND: '/404'
} as const;
