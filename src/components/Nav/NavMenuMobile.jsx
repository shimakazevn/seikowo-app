import React from 'react';
import MobileNav from './MobileNav';

const NavMenuMobile = ({ menuItems, isOpen }) => (
  <MobileNav menuItems={menuItems} isOpen={isOpen} />
);

export default NavMenuMobile; 